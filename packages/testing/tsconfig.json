{
	"extends": "../../tsconfig.roblox-ts.json",
	"compilerOptions": {
		"typeRoots": ["./node_modules/@rbxts", "./node_modules/@flamework-experimental"],
		// Pin @rbxts/t to this package's own copy. @flamework-experimental/core's declaration files import it too, and with
		// the isolated linker roblox-ts would otherwise emit that import through core's node_modules, a path
		// that exists only inside this repository (see docs/testing/studio.md).
		"baseUrl": ".",
		"paths": { "@rbxts/t": ["./node_modules/@rbxts/t"] },
		"rootDir": "src",
		"outDir": "out",
		"plugins": [
			{
				"transform": "@flamework-experimental/transformer",
				"$rbxpackmode$": true,
				"hashPrefix": "$T"
			}
		]
	}
}
